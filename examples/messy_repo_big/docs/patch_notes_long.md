# Patch Notes Long

This is a junk-drawer patch log. It mixes old decisions, new decisions, deployment notes, todos, historical patch notes, support notes, and product ideas in one long file.

Run pytest before reporting done.
Run pytest before reporting done.
Always run smoke tests before deployment.
Deploy to production only after review.
Deploy to production only after review.

Patch note block 01: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 02: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 03: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 04: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 05: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 06: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 07: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 08: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 09: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 10: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 11: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 12: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 13: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 14: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 15: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 16: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 17: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 18: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 19: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 20: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 21: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 22: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 23: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 24: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 25: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 26: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 27: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 28: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 29: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 30: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 31: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 32: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 33: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 34: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 35: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 36: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 37: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 38: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 39: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 40: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 41: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 42: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 43: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 44: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 45: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 46: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 47: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 48: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 49: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 50: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 51: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 52: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 53: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 54: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 55: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 56: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 57: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
Patch note block 58: Changed landing copy, considered Next.js, reverted to Vite, discussed Express, moved to FastAPI, debated dev-only auth, confirmed production auth active, skipped tests in one old patch, later required pytest, mentioned staging only, then approved production deploy after review.
