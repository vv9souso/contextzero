# X Launch Post

Claude keeps rereading your repo's junk drawer.

ContextZero finds it.

In the public demo fixture, `contextzero audit` found 10,158 estimated startup tokens, 7,555 estimated tokens of context to avoid, 7 stale files, 6 conflicts, and 8 duplicate instruction blocks.

It is a local CLI that scans stale handoffs, duplicate instructions, oversized agent docs, and conflicting rules before your AI coding session starts.

Powered by Caveman Brain: repo memory that remembers the important stuff and ignores the junk.

No upload. No cloud. Everything local. Token counts are estimates.
